<div class="skills app_grid one">
    <div class="htitle">
        sortilèges
    </div>
</div>

<div class="htitle app_grid">
    {% for characterSpell in character.characterSpells %}
        <div class="data_case">
            {{ characterSpell.spell.name }} ( {{ characterSpell.spell.spellType.name }} )
        </div>
    {% endfor %}
</div>